/**
 * 차트와 계획 카드 — **한 단계 밝은 카드** (Q12 · 중요도).
 *
 * 💀 머리줄 · 사슬 · 차트 · 계획 넷이 전부 같은 `card` 라 「넷 다 비슷하게 중요하다」로
 * 읽혔다. 센터 스테이지(차트)와 그 짝(계획)만 바탕을 한 단계 올리고 테두리를 준다.
 * 새 값은 안 만든다 — `bg-surface` · `shadow-card` · `border-default` 토큰이다.
 */
export const LIT_PANEL =
  'rounded-lg bg-bg-surface shadow-card ring-1 ring-border-default'
