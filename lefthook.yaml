pre-commit:
  commands:
    lint:
      run: pnpm lint
    lint-fsd:
      run: pnpm lint:fsd
    format:
      run: pnpm format
    typecheck:
      run: pnpm tsc --noEmit

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
