# API 베이스 URL. 비워두면 same-origin(`/api/...`)으로 호출하며
# dev 에서는 Vite proxy 가 백엔드로 포워딩한다.
# 운영/스테이징에서만 절대 URL 을 지정한다. 예) https://api.momentum.app
VITE_API_BASE_URL=

# dev 전용. Vite proxy 가 /api 요청을 포워딩할 백엔드 주소 (기본 http://localhost:8080)
VITE_PROXY_TARGET=http://localhost:8080
